// Fade-in animation on load
document.addEventListener("DOMContentLoaded", () => {
    document.querySelector(".container").style.opacity = "1";
    // Tambah efek lain di sini kalau mau (contoh: console.log("Portfolio loaded!"))
});
